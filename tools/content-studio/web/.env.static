# Static mode: the app resolves every /api URL to a baked data/ file.
# Used by `npm run build:static` (vite --mode static). Dev mode (npm run dev)
# omits this and talks to the FastAPI backend as before.
VITE_STATIC=1
